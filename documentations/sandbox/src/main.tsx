import * as React from 'react';
import * as ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <h1>This is just for templating.</h1>
  </React.StrictMode>,

  document.getElementById('root')
);
